[package]
name = "anneal-exec"
version.workspace = true
edition.workspace = true
description = "Execution kernel: scheduler, sandbox, materializer, warm reuse, fixed-output fetch, and the Executor interface."

[dependencies]
anneal-core = { path = "../anneal-core" }
anneal-action = { path = "../anneal-action" }
anneal-cas = { path = "../anneal-cas" }
anneal-snapshot = { path = "../anneal-snapshot" }
anneal-store = { path = "../anneal-store" }
libc = "0.2"
# Native fixed-output fetch (src/fetch.rs): pure-Rust HTTP/TLS with Mozilla's
# root store compiled in — defaults are rustls + ring + webpki-roots.
ureq = "3"

[dev-dependencies]
anneal-action = { path = "../anneal-action" }
serde_json = "1"
tempfile = "3"

[lints]
workspace = true
