[package]
name = "anneal-test"
version.workspace = true
edition.workspace = true
description = "Structured test results (§19.2) and translation from test-framework native output."

[dependencies]
anneal-core = { path = "../anneal-core" }
